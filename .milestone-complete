Task 10: Integration Tests and Final Wiring - Complete

Completed items:
1. Controller integration tests using envtest (internal/controller/suite_test.go)
   - Pool creates sandboxes (min=2 verified)
   - Sandbox creates pods with correct container specs (SDK + bridge)
   - Workflow DAG execution (diamond: A -> B, A -> C, B+C -> D)
   - Task claims Ready sandbox and transitions to Running
   - Scale-up triggered when active ratio exceeds threshold

2. All three binaries build successfully:
   - bin/controller-manager
   - bin/apiserver
   - bin/bridge

3. CRD manifests generated to config/crd/bases/ (make manifests target exists)

4. Multi-stage Dockerfile for all binaries

5. Final review:
   - go vet ./... passes
   - go build ./... passes
   - go test ./... -count=1 passes (all packages)
