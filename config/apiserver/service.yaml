apiVersion: v1
kind: Service
metadata:
  name: apiserver
  labels:
    app.kubernetes.io/name: apiserver
    app.kubernetes.io/part-of: software-factory
spec:
  selector:
    app.kubernetes.io/name: apiserver
  ports:
    - port: 8080
      targetPort: http
      name: http
      protocol: TCP
